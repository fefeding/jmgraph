/**
 * @fileoverview 流程图示例入口（转发）
 *
 * 转发到 jmGraph 官方组件统一入口 `src/components`，使示例直接演示
 * 官方组件 API（`createFlowGraph` 返回 `FlowGraphComponent` 实例）。
 * 核心实现见 `src/components/flow/flow-graph.js`。
 */
export * from '../../src/components/index.js';
